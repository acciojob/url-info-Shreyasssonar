//your JS code here. If required.
// Get the current URL using the location object
var currentURL = window.location.href;

// Get the length of the URL using the length property
var urlLength = currentURL.length;

// Display the length using the alert function
alert("The length of the URL is: " + urlLength);
